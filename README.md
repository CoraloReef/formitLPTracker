# LPTracker in MODX Formit

Create lead in LPTracker from MODX Formit forms

Installation with Composer:
```php
composer require lptracker/php-sdk
```

Create snippet "lptracker" and add hook in Formit
```php
&hooks=`email,lptracker`
```